namespace TrashTD.Enemies
{
    /// <summary>
    /// Grunt enemy — basic ground unit (GDD 1.5).
    /// Standard behavior, no special mechanics. Countered by any DPS.
    /// </summary>
    public class GruntEnemy : EnemyBase
    {
        // Grunt uses all default EnemyBase behavior — no overrides needed.
        // This is the baseline enemy that all others are measured against.
    }
}
